// implementation coming soon

// IN the Srp file, I have used, 

// export interface TeamRegistration {
//     calculateFeeFunction(team: Team) : void;
//     registerTeam(team: Team): void;
//     updateTeamList(): void;
//     deleteMember(id: string): void;
//     deleteAllTeamMembers(): void;
//   }
  
// export interface EmailFunctionality {
//     sendEmail(email: Email): void;
//   }
//  This ensures that any class implementing the TeamRegistration interface will have to implement 
// all the methods compulsorily, otherwise it will throw an error. This is a good practice to follow.
// Still, Typescript allows us to define new methods in the class which are not defined in the interface. 


